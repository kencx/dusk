package partials

import (
    "dusk"
)

templ Library(books dusk.Books, errorMessage string) {
    <div class="library__card-view">
    if len(books) <= 0 {
        <p>{errorMessage}</p>
    } else {
        for _, book := range books {
            @bookCard(book)
        }
    }
    </div>
}
